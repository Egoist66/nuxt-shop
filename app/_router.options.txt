import type { RouterConfig } from "nuxt/schema";

export default {

    routes(_routes) {
        return [
            {
                name: 'home',
                alias: '/home',
                path: '/',
                component: () => import('~/pages/index.vue')
            }
        ]
    }

} satisfies RouterConfig